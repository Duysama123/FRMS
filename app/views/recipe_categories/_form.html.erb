<%= form_with(model: recipe_category) do |form| %>
<% if recipe_category.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(recipe_category.errors.count, "error") %> prohibited this recipe_category from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% recipe_category.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :recipe_id, class: label_class %>
          <%= form.text_field :recipe_id, class: input_class %>
      </div>
  <div class="mb-6">
    <%= form.label :category_id, class: label_class %>
    <%= form.select :category_id, options_from_collection_for_select(Category.all, :id, :name, selected = recipe_category.category_id), class: input_class %>
  </div>


<%= form.submit class: button_class(theme: 'primary') %>

<% if recipe_category.persisted? %>
<%= link_to "Cancel", recipe_category, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", recipe_categories_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
