<head>
  <title>
    <%= content_for?(:title) ? yield(:title) : "Cooking Healthy Food" %>
  </title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag  'application', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag "application", defer: true %>

  <!-- Add the favicon -->
  <link rel="shortcut icon" type="image/png" href="<%= asset_path('photo.png') %>">
</head>

 
